{
  "repository": "https://github.com/UseJunior/safe-docx",
  "trackingBranchName": "main",
  "note": "Source-built from the tip of the tracking branch on every suite run so safe-docx changes are re-tested without an npm release; install.sh resolves the branch to a commit and records it in build-info.json. To reproduce a past matrix, set pinnedCommitSha (takes precedence over trackingBranchName)."
}
